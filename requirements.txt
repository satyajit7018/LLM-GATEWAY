fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
redis==5.0.7
httpx==0.27.0
python-dotenv==1.0.1
numpy==1.26.4
prometheus-client==0.20.0
pypdf==4.2.0
jedi==0.19.2                 # Python IntelliSense for the code editor (/lsp/python)
cryptography==43.0.3         # encrypts users' bring-your-own provider API keys at rest

# --- Optional (real backends / canonical tooling) --------------------------
# Semantic cache with true paraphrase matching (Step 4). Heavy (pulls torch);
# the default lexical embedder needs none of this. Enable with EMBED_BACKEND=sbert.
# sentence-transformers==2.7.0
#
# Canonical Step 6 load-test tool. scripts/loadtest.py covers this with no install.
# locust==2.29.1

# --- Dev / test ------------------------------------------------------------
pytest==8.2.2
# Frontend smoke tests (tests/test_frontend_smoke.py) — Python bindings, no
# Node/npm needed. Requires a one-time `playwright install --with-deps chromium`
# after pip install (see README / CI).
pytest-playwright==0.5.1

